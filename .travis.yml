sudo: false

language: php

services:
  - docker

php:
  - 7.2

before_script:
    - cd microservices/application/user_management/
    - travis_retry composer self-update
    - travis_retry composer install --no-interaction --prefer-source --dev
  
script:
  - composer test tests/UserTestCase.php
  
install: true
